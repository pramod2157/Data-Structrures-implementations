package greedyMethods;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;

public class ArjitAndPrintingPress {

	public static void Main(String[] args) throws IOException {
/*		
		BufferedReader br=new BufferedReader(new InputStreamReader(System.in));
		int NoOfTests=Integer.parseInt(br.readLine());
		for(int i=0;i<NoOfTests;i++)
		{
			String word=br.readLine();
			char[] temp=new char[word.length()];
			String reverseWord=br.readLine();
			for(int )
			int iteration=word.length()<reverseWord.length()?word.length():reverseWord.length();
			for(int j=0;j<iteration;j++)
			{
				if(word.charAt(j)>reverseWord.charAt(j))
				{
					temp[j]=reverseWord.charAt(j);
					
				}
				else{
					temp[j]=word.charAt(j);
					
				}
				
				
			}
			for(int j=iteration;j<word.length();j++)
				temp[j]=word.charAt(j);
			
			String finalone=new String(temp);
			System.out.println(finalone);
		}
*/		
	}

}
